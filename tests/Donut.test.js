// import roughBars from '../src';

describe('Test Chart', () => {
  test('Attributes should correctly propagate', () => {


    // let padding = barChart.padding;
    const padding = 0.15;


    // const output = [{ id: 3, url: "https://www.link3.dev" }];
    expect(padding).toEqual(0.15);
  });
});
